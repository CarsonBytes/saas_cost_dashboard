[project]
name = "saas-cost-dashboard"
version = "0.1.0"
description = "Personal SaaS cost dashboard + ops hub (NiceGUI + Supabase)"
requires-python = ">=3.12"
dependencies = [
    "nicegui==3.14.0",
    "httpx==0.28.1",
    "python-dotenv==1.2.2",
]
